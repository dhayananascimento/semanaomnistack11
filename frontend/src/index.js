import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//coloca em tela o componente app no documento onde possui o id 'root'
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


